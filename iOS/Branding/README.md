# ClashNL Apple Branding

`ClashNL-logo-1024.png` 是 ClashNL 的标准品牌源图。

Apple 的普通、深色和 tinted 图标由下列脚本生成：

```bash
python scripts/generate-app-icon.py
```

生成结果位于 `SFI/Assets.xcassets/AppIcon.appiconset/`，标准版本同时同步回
本目录。
