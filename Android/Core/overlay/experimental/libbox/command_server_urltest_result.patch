diff --git a/experimental/libbox/command_server.go b/experimental/libbox/command_server.go
index 312431b3..3153b3c9 100644
--- a/experimental/libbox/command_server.go
+++ b/experimental/libbox/command_server.go
@@ -208,6 +208,13 @@ func (s *CommandServer) CloseService() error {
 	return s.StartedService.CloseService()
 }
 
+// URLTest runs one synchronous URL test for a concrete outbound and returns
+// the measured end-to-end delay in milliseconds.
+func (s *CommandServer) URLTest(outboundTag string) (int32, error) {
+	delay, err := s.StartedService.URLTestResult(outboundTag)
+	return int32(delay), err
+}
+
 func (s *CommandServer) WriteMessage(level int32, message string) {
 	s.StartedService.WriteMessage(log.Level(level), message)
 }
