ClashNL Android v1.0.6.

- Use IPv4 exclusively for account login and subscription HTTP(S) traffic.
- Reject IPv6-only resolution instead of retrying the same request over IPv6.
- Keep the concise retry message for transport-level failures.
